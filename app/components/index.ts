/**
 * Components barrel file
 * 
 * Following BuildTrack Pro's component organization standards, this exports
 * component directories to facilitate imports using the module resolution pattern.
 */

import * as shared from './shared';
import * as profile from './profile';

export {
  shared,
  profile
};
